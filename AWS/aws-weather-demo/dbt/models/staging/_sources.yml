version: 2

sources:
  - name: raw
    description: "Raw NDJSON landed by the ingestion Lambda; Glue external table (OpenX SerDe + partition projection)."
    schema: weather_demo
    tables:
      - name: raw_weather
        description: "One row per city per ingestion run. All columns string; typed/normalized in staging."
