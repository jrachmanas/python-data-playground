# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log
*.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# NOTE: .terraform.lock.hcl is intentionally NOT ignored — commit it so provider
# versions are reproducible across machines/CI.

# Python / dbt local venv
.venv/
__pycache__/
*.pyc

# dbt run artifacts
dbt/logs/
dbt/target/
dbt/dbt_packages/

# Lambda build artifacts
ingestion/*.zip

# macOS
.DS_Store
